import React from "react";
import { View, Text } from "react-native";

const Main = props => {
  console.log(props);
  return (
    <View>
      <Text>main</Text>
    </View>
  );
};

export default Main;
