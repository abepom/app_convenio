import {primary} from './Style';

const themeDark = {
  colors: {
    primary: '#fff',

    background: primary,

    text: '#fff',
    placeholder: '#fff',
  },
};

export const themeLight = {
  colors: {
    primary: primary,

    background: '#fff',

    text: primary,
    placeholder: primary,
  },
};

export default themeDark;
